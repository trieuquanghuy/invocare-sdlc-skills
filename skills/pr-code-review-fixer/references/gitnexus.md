# Merged into reposphere.md

gitnexus is not configured in this workspace. The caller/callee and cross-file blast-radius guidance that used to live here now lives in `reposphere.md` ("Caller/callee evidence (single repo)"), built on `mcp__reposphere__explore_neighborhood`, `mcp__reposphere__graph_query`, and `mcp__reposphere__get_review_context`.
